<div class="loading-overlay"><div class="spin"><i class="ion-radio-waves"></i></div></div>

{{!-- {{ confirm-modal-unsaved }} --}}
{{!-- {{ confirm-modal-delete }} --}}

{{#if session.isAuthenticated}}
    {{!-- User is logged in: display the application, with wrapping elements --}}
    {{ partial 'application/-nav' }}

    <div class="container-fluid">
        <div class="row">
            <div class="col-xl-12">
                {{ outlet }}
            </div>
        </div>
    </div>
{{else}}
    {{!-- User is not logged in: show the splash screen, without wrappers --}}
    {{login-splash}}
{{/if}}
